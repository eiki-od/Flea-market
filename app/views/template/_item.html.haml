.list-new
  - @posts.each do |post|
    = link_to "#" do
      .product
        .img-new
          -# - @post.images.each do |image|
            = image_tag image.src.url
        .info
        %p.post-name-top 
          = post.name 
          %br/
          ¥
          - price = post.price*1.1
          = price.truncate
          (税込)
          %i.fas.fa-star
            0